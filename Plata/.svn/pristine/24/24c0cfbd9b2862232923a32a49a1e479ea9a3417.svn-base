SET PROCEDURE TO MENURAIZ

DO FORM FMIRROR

READ EVENTS

PROC TEMPFILE
*************
LPARAMETERS tcEXT

	IF VARTYPE(tcEXT) <> 'C'
		tcEXT = 'TMP'
	ENDIF

	LOCAL lcPATH AS String
	
	lcPATH = ADDBS(GETENV("TEMP")) + SYS(3) + '.' + tcEXT
	
	RETURN( lcPATH )
ENDPROC